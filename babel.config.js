module.exports = {
    presets: [
        "@babel/preset-env",
        "@babel/preset-react"
    ],
    plugins: [
        '@babel/plugin-proposal-class-properties'
    ]
}

// preste-env = pro babel entender a nova semântica, "import export"